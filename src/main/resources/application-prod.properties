spring.application.name=atlasmentor

# PostgreSQL Database Configuration - Production
spring.datasource.url=jdbc:postgresql://localhost:5432/atlas_mentor
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=Root@123

# HikariCP Connection Pool - Production
spring.datasource.hikari.pool-name=AtlasMentor-Prod
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=300000

# JPA/Hibernate Configuration - Production
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Explicitly off — belt-and-suspenders on top of the safe defaults in application.properties.
# Both paths (show-sql stdout + hibernate SLF4J logger) must be suppressed independently.
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.orm.jdbc.bind=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Email Configuration - Production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=atlasmentor2026@gmail.com
spring.mail.password=pgvr whvu pnpg ftpp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration — override with JWT_SECRET env var in production
jwt.secret=${JWT_SECRET:EHrhUGsie6Lf/hdP9FNUaPM+7MZBVS29wtU+e53t0i8=}
jwt.expiration=3600
jwt.refresh-expiration=604800

# Application Configuration - Production
app.frontend.url=http://65.0.103.210

# Logging: levels and format are controlled by logback-spring.xml (prod profile)
